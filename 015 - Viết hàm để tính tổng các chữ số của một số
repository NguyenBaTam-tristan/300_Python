def sum_digits(n):
    n = abs(n)
    total = 0
    while n > 0:
        total += n%10
        n//=10
    return total
try:
    n=int(input('Điền số:'))
    print(f'Tổng các chữ số trong {n} là {sum_digits(n)}')
except ValueError:
    print('Điền số hợp lệ')
